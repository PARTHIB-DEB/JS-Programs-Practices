// There are 2 ways of Declaring Numbers in Js
let numVar = 20;
console.table([numVar,typeof numVar])

numVar = new Number(20);
console.log(numVar);
console.table([Number(numVar.toString()),typeof numVar])
